const verses = require('./verses');
const search_keys = require('./search_keys');
const notes = require('./notes');
const decks = require('./decks');
const users = require('./users');


module.exports = {
  verses,
  search_keys,
  notes,
  users,
  decks
};